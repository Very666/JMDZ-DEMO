spring:
  application:
    name: client-three
server:
  port: 8093
feign:
  hystrix:
    enabled: true
management:
  endpoints:
    web:
      exposure:
        #\u66B4\u9732\u7AEF\u70B9
        include: hystrix.stream,bus-refresh